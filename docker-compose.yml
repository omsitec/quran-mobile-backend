version: '3.8'

services:
  quran-backend:
    build: .
    container_name: quran-mobile-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - quran-network

networks:
  quran-network:
    driver: bridge
